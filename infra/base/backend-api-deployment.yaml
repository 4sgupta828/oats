apiVersion: apps/v1
kind: Deployment
metadata:
  name: oats-backend-api
  labels:
    app: oats-backend-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: oats-backend-api
  template:
    metadata:
      labels:
        app: oats-backend-api
    spec:
      serviceAccountName: oats-job-creator # Important: Use the SA defined in rbac.yaml
      containers:
        - name: backend-api
          image: oats-backend-api:v3
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          env:
            - name: AGENT_IMAGE
              value: "oats-agent:v4"
            - name: API_KEY_SECRET_NAME
              value: "oats-api-keys"