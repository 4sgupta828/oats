# Pydantic for data models, used heavily in your reactor and core modules
pydantic

# LLM Clients
openai
anthropic

# Used for accurate token counting in the prompt builder
tiktoken

# For loading .env files during local testing (optional but good practice)
python-dotenv